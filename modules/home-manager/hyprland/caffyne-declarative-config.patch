diff --git a/main.py b/main.py
--- a/main.py
+++ b/main.py
@@ -12,5 +12,9 @@
 singletons.style_service = StyleService(app)
-
+
 singletons.style_service.reload()
-
+
+# UserOptions is the durable source of truth. Re-apply it on every startup so
+# externally reconciled theme and template selections regenerate their CSS.
+singletons.theme_service.apply()
+
 bar_manager = bar.initialise_bars()
