diff --git a/bar.py b/bar.py
index 441c741..540b8eb 100644
--- a/bar.py
+++ b/bar.py
@@ -28,7 +28,6 @@ from utils.monitors import get_connector_from_monitor_id
 from utils.sounds import play_sound
 
 BAR_WIDGETS: dict[str, type] = {
-    "Launcher":      LauncherButton,
     "Processes":     CPUIndicatorButton,
     "Energy":        BatteryButton,
     "Bluetooth":     BluetoothButton,
@@ -65,7 +64,6 @@ APPLET_WIDGETS: dict[str, type] = {
     "Energy":        PowerApplet,
     "Calculator":    CalculatorApplet,
     "Keyboard":      KeyboardApplet,
-    "Launcher":      LauncherApplet,
     "Processes":     ProcessMonitorApplet,
 }
 
@@ -76,7 +74,6 @@ INCOMPATIBLE_GROUPS: set[frozenset] = {
     frozenset({"Settings", "Volume"}),
     frozenset({"Settings", "Keyboard"}),
     frozenset({"Settings", "Session"}),
-    frozenset({"Processes", "Launcher"}),
 }
 from plugin_loader import load_plugins
 from windows.dash.applets import ALL_BEAN_DATA
diff --git a/bar_widgets/dash.py b/bar_widgets/dash.py
index 1992c9c..95394de 100644
--- a/bar_widgets/dash.py
+++ b/bar_widgets/dash.py
@@ -17,6 +17,6 @@ class DashButton(BaseButton):
                 return False
             import services.singletons as singletons
             if singletons.bar_manager:
-                singletons.bar_manager.toggle("Dash")
+                singletons.bar_manager.toggle("EditApplets")
                 self.get_parent().remove_style_class("active")
             return True
\ No newline at end of file
diff --git a/user_options.py b/user_options.py
index 3b1e1ab..34304da 100644
--- a/user_options.py
+++ b/user_options.py
@@ -30,7 +30,6 @@ class UserOptions:
                             "auto_hide": False,
                             "left": [
                                 "Dash",
-                                {"widget": "Launcher", "variant": "icon"},
                                 {"widget": "Processes", "variant": "scale"},
                                 "Weather",
                                 "Media"
@@ -60,7 +59,6 @@ class UserOptions:
                             "auto_hide": False,
                             "left": [
                                 "Dash",
-                                {"widget": "Launcher", "variant": "icon"},
                                 {"widget": "Processes", "variant": "scale"},
                                 "Weather",
                                 "Media"
diff --git a/windows/dash/applets.py b/windows/dash/applets.py
index d9b253c..908d177 100644
--- a/windows/dash/applets.py
+++ b/windows/dash/applets.py
@@ -14,7 +14,6 @@ import cairo
 
 ALL_BEAN_DATA: list[tuple[str, str]] = [
     ("caffyne-duotone",                 "Dash"),
-    ("magnifying-glass-duotone",        "Launcher"),
     ("dock-duotone",                    "Dock"),
     ("cards-three-duotone",             "Workspaces"),
     ("app-window-duotone",              "Focused"),
diff --git a/windows/dash/components.py b/windows/dash/components.py
index e16431d..9880d28 100644
--- a/windows/dash/components.py
+++ b/windows/dash/components.py
@@ -40,6 +40,7 @@ class DashHeader(CenterBox):
         v_icon: str,
         v_callback,
         show_search: bool = False,
+        show_peer: bool = True,
         current_on_left: bool = True,
         h_switcher_on_right: bool = False,
     ):
@@ -77,18 +78,22 @@ class DashHeader(CenterBox):
 
             if current_on_left:
                 self._right_box.add(current_btn)
-                self._right_box.add(peer_btn)
+                if show_peer:
+                    self._right_box.add(peer_btn)
             else:
-                self._right_box.add(peer_btn)
+                if show_peer:
+                    self._right_box.add(peer_btn)
                 self._right_box.add(current_btn)
             self._right_box.show_all()
         else:
 
             if current_on_left:
                 self._left_box.add(current_btn)
-                self._left_box.add(peer_btn)
+                if show_peer:
+                    self._left_box.add(peer_btn)
             else:
-                self._left_box.add(peer_btn)
+                if show_peer:
+                    self._left_box.add(peer_btn)
                 self._left_box.add(current_btn)
             self._left_box.show_all()
 
diff --git a/windows/dash/dash.py b/windows/dash/dash.py
index 1cd793b..b145a64 100644
--- a/windows/dash/dash.py
+++ b/windows/dash/dash.py
@@ -171,6 +171,7 @@ class Dash(Window):
 
         self.h_group_1.add_named(self.launcher,   "apps")
         self.h_group_1.add_named(self.applets,    "applets")
+        self.h_group_1.set_visible_child(self.applets)
         self.h_group_2.add_named(self.wallpapers, "wallpapers")
         self.h_group_2.add_named(self.themes,     "themes")
         self.v_stack.add_named(self.h_group_2,    "themes-wallpapers")
@@ -228,7 +229,7 @@ class Dash(Window):
         if not self.is_visible():
             return
         self._applet_drag_key = key
-        self.dismiss_layer.show_drop_zones(key, show_left=show_left, show_right=show_right)
+        self.dismiss_layer.show_drop_zones(key, show_left=False, show_right=show_right)
 
     def _on_applet_drag_end(self) -> None:
         self._applet_drag_key = None
@@ -321,6 +322,7 @@ class Dash(Window):
             v_icon=v_icon,
             v_callback=lambda: self.v_stack.set_visible_child_name(v_target),
             show_search=(name in _PAGES_WITH_SEARCH),
+            show_peer=(name != "applets"),
             current_on_left=current_on_left,
             h_switcher_on_right=(name in ("wallpapers", "themes")),
         )
@@ -339,7 +341,7 @@ class Dash(Window):
 
     def _on_v_stack_changed(self, *_):
         self._on_stack_changed()
-        self.h_group_1.set_visible_child(self.launcher)
+        self.h_group_1.set_visible_child(self.applets)
         self.h_group_2.set_visible_child(self.wallpapers)
 
 
@@ -409,5 +411,5 @@ class Dash(Window):
         if self._in_canvas_mode:
             self._exit_canvas_mode()
         self.v_stack.set_visible_child(self.h_group_1)
-        self.h_group_1.set_visible_child(self.launcher)
+        self.h_group_1.set_visible_child(self.applets)
         edit_mode.disable()
\ No newline at end of file
